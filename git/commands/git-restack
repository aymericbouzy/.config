#! /bin/zsh

base=$(git base)
local new_base="$1"

git ff "$base"
if [ "$base" = master ] && [ -z "$new_base" ]; then
	git merge master
else
	git rb
fi

if [ $? -ne 0 ]; then
	return $?
fi

if [ ! -z "$new_base" ]; then
	git ff "$new_base"
	git base "$new_base"
	git rb
fi
